# Linting and Formatting

## JavaScript/React
- Run `npx eslint .` to lint JS/JSX files
- Run `npx prettier --check .` to check formatting
- Run `npx prettier --write .` to auto-format

## Python
- Run `flake8 .` to lint Python files

## EditorConfig
- Most editors will auto-detect `.editorconfig` for consistent indentation
